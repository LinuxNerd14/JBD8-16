MEMORY {
    ZP:  start = $0000, size = $0100, type = rw;  # Zero page
    RAM: start = $4481, size = $7D00, type = rw;  # RAM (4481-C181)
    ROM: start = $C182, size = $37DE, type = ro;  # ROM (C182-FFFF)
}
SEGMENTS {
    CODE: load = ROM, type = ro, define = yes;
}
